# ─── ITEC EAD — Variáveis de Ambiente ──────────────────────────────────────
# Copie este arquivo para .env e preencha com os valores reais.
# NUNCA commite o arquivo .env — ele já está no .gitignore.

# Supabase — obtenha em: https://supabase.com/dashboard → Settings → API
VITE_SUPABASE_URL=https://SEU_PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=sua_anon_key_aqui

# URL pública do site (sem barra final)
VITE_SITE_URL=https://www.itecedu.com

# E-mail do superadmin — usuário que recebe role 'superadmin' independente do banco
# Nunca commitar o valor real — preencher no .env local e nas env vars da Vercel
VITE_SUPERADMIN_EMAIL=
