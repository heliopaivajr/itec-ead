# Migrações aplicadas — FONTE DA VERDADE
## ITEC-EAD · estado real do banco de produção

> ⚠️ **LEIA ANTES DE DIZER QUE UMA MIGRAÇÃO ESTÁ "PENDENTE".**
> O Supabase MCP **NÃO enxerga** o projeto ITEC (conta diferente). Logo, o estado real
> do banco **não é verificável por ferramenta** — esta lista é a única fonte da verdade
> sobre o que já rodou. Sempre consultar este arquivo antes de afirmar status de migração.
>
> Quem aplica: **somente o Hélio**, no SQL Editor (ERR-INFRA-001). Claude Code cria o `.sql`, nunca roda.
> Atualizado: 2026-06-20.

---

## Convenção
- **APLICADA** = já rodada por Hélio no SQL Editor e validada. **NÃO reexecutar.**
- **CRIADA (aguardando run)** = arquivo `.sql` existe no repo, mas Hélio ainda não rodou.
- Em caso de dúvida, perguntar ao Hélio — não assumir.

---

## Núcleo Acadêmico (trilha R0 → R4)

| Migração | Status | Quando | Validação |
|----------|--------|--------|-----------|
| `supabase/migrations/20260619_047_r0_reconciliacao_prerequisitos.sql` | ✅ **APLICADA** | 2026-06-19/20 | 24/24/24 (códigos v2 alinhados; 24 pré-req re-apontados para `disciplinas_v2`). **NÃO reexecutar.** |
| `supabase/migrations/20260620_048_r0_5_consolida_curriculo_v2.sql` | ✅ **APLICADA** | 2026-06-20 | `prerequisitos_v2` repopulada: **13 `prerequisito` + 11 `recomendado`**; coluna `disciplinas_v2.ativo` criada; CHECK ampliado com `recomendado`. **NÃO reexecutar.** |
| `supabase/migrations/20260622_049_materiais_disciplina.sql` | ✅ **APLICADA** | 2026-06-22 | R0.5.4: tabela `materiais_disciplina` + RLS, função `aluno_ve_disciplina()`, bucket privado `materiais-disciplina` + policies. Versionada no `main` (PR #16 merged). **NÃO reexecutar.** |
| `supabase/migrations/20260622_050_manual_aluno.sql` | ✅ **APLICADA** | 2026-06-22 | Manual do Aluno (handbook): coluna `cursos.manual_aluno_url` + bucket privado `manuais-aluno` (read authenticated; write admin/superadmin). Validada 1/1/4 (coluna + bucket + 4 policies). Versionada no `main` (PR #17 merged). **NÃO reexecutar.** |
| `supabase/migrations/20260623_051_r1_schema_retroativo.sql` | ✅ **APLICADA** | 2026-06-23 | R1: `matriculas.numero_matricula` (nullable + índice único parcial) + CHECK do funil ADITIVO (7+5 valores) + função `gerar_numero_matricula()` (advisory lock + ano via `lpad`). `matriculas_disciplina` ganha `faltas`, `frequencia_percentual`, `observacao` e `convalidacao_*` (5 col). Validada: colunas criadas; CHECK aditivo; `gerar_numero_matricula()` testada (2025→`ITEC25T001`, 2026→`ITEC26T001`). **NÃO reexecutar.** ⚠️ Os 5 `convalidacao_*` foram **removidos pela 052** (redundantes). 📌 **Versionada em 2026-07-05** (branch `chore/versionar-migrations-pendentes`) — estava aplicada mas SEM cópia no repositório (achado da auditoria 2026-07-05, Ressalva 0). |
| `supabase/migrations/20260627_052_r2_professor_id_drop_convalidacao.sql` | ✅ **APLICADA** | 2026-06-27 | R2 (G1+G2): `matriculas_disciplina.professor_id` UUID FK→`professores`; **DROP** dos 5 `convalidacao_*` da 051 (redundantes — convalidação canônica = tabela `convalidacoes`). Validada: `professor_id` criado=1; `convalidacao_*` restantes=0. **NÃO reexecutar.** |
| `supabase/migrations/20260627_053_recuperacao_status_check.sql` | ✅ **APLICADA** | 2026-06-27 | R2.2: adiciona `'recuperacao'` ao CHECK de `matriculas_disciplina.status` (aditivo — 6 atuais + recuperacao). Validada: CHECK agora inclui `recuperacao`; lançamento retroativo de cadeiras em recuperação destravado. Versionada no branch do R2.2. **NÃO reexecutar.** |
| `supabase/migrations/20260706_054_sec01_revoke_user_roles.sql` | ✅ **APLICADA** | 2026-07-06 | **SEC-01 (report-B, BLOQUEADOR) — RESOLVIDO:** REVOKE de INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER na VIEW `user_roles` p/ anon+authenticated e REVOKE SELECT p/ anon — fecha a escalação de privilégio via UPDATE na view (que executava como dona e bypassava o RLS de profiles). ⚠️ `security_invoker=true` foi **excluído de propósito** — reintroduziria a recursão infinita do ADR-006 (P2 de profiles consulta user_roles). Validada: `anon` sem nenhum privilégio na view; `authenticated` só com `SELECT` (policies continuam funcionando). **NÃO reexecutar.** Versionada via PR `fix/sec01-user-roles`. |
| `supabase/migrations/20260707_055_storage_rls_hardening.sql` | ✅ **APLICADA** | 2026-07-07 | **SEC-03 + SEC-05 + SEC-04 parcial + manual do aluno editável (report-B).** Buckets privados com 0 objetos (sem risco). (1) `aluno_ve_disciplina` agora exige `status IN ('ativa','concluida')` — **SEC-05 fechado**. (2) `materiais_obj_select` gate por disciplina via `foldername[1]::uuid` + staff/professor leem tudo — **SEC-03 fechado**. (3) escrita de materiais inclui `administracao` — **SEC-04 parcial** (professor fica p/ o sprint com UI). (4) `manuais_aluno_write/update/delete` incluem `administracao` (secretaria/coordenação sobem e revisam handbook). Validada: policies vigentes conferidas; writes com `administracao`; função com filtro de status. **NÃO reexecutar.** Rollback (recria 049/050) no próprio arquivo. Versionada via PR `fix/storage-055-rls`. |

| `supabase/migrations/20260707_056_material_professor.sql` | ✅ **APLICADA** | 2026-07-07 | **Sprint Material do Professor (SEC-04 metade 2 + gap da 055) — validada.** (1) Funções `professor_leciona_disciplina` (contrato ativo) e `professor_dono_contrato` — SECURITY DEFINER. (2) Tabela `materiais_disciplina`: policies do professor (SELECT/INSERT/UPDATE/DELETE na cadeira do contrato) + `administracao` no `staff_all` (**fecha o gap da 055**: administracao subia arquivo mas a linha falhava). (3) Storage materiais: ramo do professor no write/update/delete. (4) Bucket privado `contratos-professor` + policies (professor lê/sobe o PDF do próprio contrato; staff tudo; delete só staff). (5) `contratos_professor`: professor faz UPDATE da própria linha com WITH CHECK travando status/professor_id/disciplina_id (só `pdf_url` muda — quem marca 'assinado' é a secretaria). Validada: funções criadas (SECURITY DEFINER); policies conferidas; bucket `contratos-professor` privado. **NÃO reexecutar.** Rollback (volta pós-055) no arquivo. Versionada via PR `feat/material-professor`. |

| `supabase/migrations/20260708_057_get_alunos_operacional.sql` | ✅ **APLICADA** | 2026-07-08 | **Roster do professor + LGPD-01 fase 1 — validada** (assinatura correta; gate `professor_leciona_disciplina` OR staff; sem `anon`; retorna operacional sem email). **NÃO reexecutar.** Versionada via PR `fix/057-roster`. Função `get_alunos_operacional(disciplina_id)` — SQL STABLE SECURITY DEFINER search_path=public. Retorna alunos matriculados da disciplina com SÓ operacional (`full_name`, `avatar_url` — **sem email** por minimização; sem cpf/rg/endereço) + snapshot acadêmico (`nota`/`faltas`/`frequencia_percentual`/`status`). Gate no corpo: `professor_leciona_disciplina` (056, contrato ativo) OU staff (admin/superadmin/administracao) — senão 0 linhas. `REVOKE EXECUTE ... FROM anon`. **1 função, nenhuma tabela alterada.** Validado: tipos batem (nota DECIMAL→numeric, faltas INT→integer, freq NUMERIC→numeric, status VARCHAR→text); sem soft-delete a filtrar. Alicerce de Meus Alunos + semear roster em Frequência/Notas. Fase 2 (endurecer `profiles_select_staff`) vem depois. Verificação + rollback no arquivo. |

| `supabase/migrations/20260709_058_roster_turma_aware.sql` | ✅ **APLICADA** | 2026-07-09 | **Roster turma-aware (IDEAS-BACKLOG 2.07) — validada** (só a 2-arg existe; anon sem EXECUTE; MeusAlunos segue com 1 arg via default). **NÃO reexecutar.** Versionada via PR `fix/058-turma-aware`. Substitui a 057: **DROP** de `get_alunos_operacional(uuid)` + **CREATE** de `get_alunos_operacional(uuid, uuid DEFAULT NULL)` — 2º param `p_turma_id` opcional (`NULL` = todas as turmas; filtro via `matriculas.turma_id`). ⚠️ As duas assinaturas NÃO podem coexistir (chamada de 1 arg fica ambígua → PGRST203); por isso a 1-arg é dropada. **MeusAlunos/LancarFrequencia/VerTurma seguem chamando com só `p_disciplina_id`** (PostgREST preenche o default NULL) — sem mudança de código. Notas passará a chamar com `p_turma_id` (código à parte). Gate `professor_leciona_disciplina` mantido; `REVOKE anon`. Verificação + rollback (volta à 1-arg da 057) no arquivo. |

| `supabase/migrations/20260710_059_policies_frequencia_notas.sql` | ✅ **APLICADA** | 2026-07-10 | **Camada 0 do módulo Freq/Notas — 🔴 SEC-06 RESOLVIDO — validada** (3 policies conferidas via `pg_policies`; auditoria: **0 notas forjadas** por role aluno). **NÃO reexecutar.** Versionada via PR `fix/059-policies-sec06`. (aluno inseria a própria nota via `auth.uid()=lancado_por`). Só policies, nada de tabela/dado: (1) `notas_aluno_insert/update` → gate real `professor_leciona_disciplina(disciplina_id)` OU staff **incluindo `administracao`** (matriz §5) + `lancado_por = auth.uid()` no WITH CHECK (autoria). (2) `frequencia_professor_update` NOVA → professor da cadeira corrige chamada (ramo ON CONFLICT do upsert) e `justificarFalta`. SELECT/DELETE de notas e INSERT/ALL de frequência inalterados. ⚠️ Auditoria pré: `count(*)` de notas com `lancado_por` de role aluno — esperado 0. Verificação por papel + rollback (031; reabre SEC-06) no arquivo. |

| `supabase/migrations/20260713_060_roster_codigo_itec.sql` | ✅ **APLICADA** | 2026-07-13 | **Roster +codigo_itec (LGPD-01 fase 2 · Mov.2) — validada** (roster retorna `codigo_itec`; Meus Alunos/Frequência seguem OK). **NÃO reexecutar.** Versionada via PR `fix/060-roster-codigo-itec`. Pré-requisito do R02 escopado: `get_alunos_operacional` (058) não retornava `codigo_itec`; o R02 (Lista de Presença) exibe o "Cód. ITEC" e o lia de `profiles` direto — o Mov.2 troca essa leitura pelo roster. **DROP + CREATE** (Postgres não faz CREATE OR REPLACE mudando colunas do RETURNS) espelhando a 058: assinatura idêntica `(uuid, uuid DEFAULT NULL)`, adiciona `codigo_itec text` **após `avatar_url`** (posição só de legibilidade — PostgREST devolve JSON por nome, não posicional) + `pr.codigo_itec` no SELECT. Gate/filtro/STABLE SECURITY DEFINER/search_path inalterados; reaplica `REVOKE EXECUTE ... FROM anon`. **Aditivo — não quebrou consumidores** (MeusAlunos/LancarFrequencia/VerTurma/getConsolidadoTurma acessam por nome de campo e ignoram a coluna nova). Pré-condição confirmada: `profiles.codigo_itec` = TEXT UNIQUE nullable (migração 030). Verificação + rollback (volta à 058, só após reverter o código do R02) no arquivo. |

| `supabase/migrations/20260714_061_frequencia_policies_insert.sql` | ✅ **APLICADA** | 2026-07-14 | **Bug A1b — restaura o conjunto completo de policies de `frequencia` — validada** (5 policies conferidas; **professor salva chamada** ✓). **NÃO reexecutar.** Versionada via PR `chore/migrations-061-064`. Produção estava com UMA policy só (`frequencia_professor_update`) — sem INSERT ninguém salvava chamada ("new row violates RLS"), sem SELECT ninguém lia. Recriou 5: `frequencia_staff_all` (ALL, staff via user_roles) · `frequencia_select_aluno_propria` (aluno vê a própria) · `frequencia_select_professor_disciplina` (professor da cadeira) · `frequencia_professor_insert` (INSERT endurecido: `professor_id = auth.uid()` + `professor_leciona_disciplina`) · `frequencia_professor_update` (idem no WITH CHECK). DELETE = só staff. Endurecimento vs 031 (INSERT era role-only: qualquer professor lançava em qualquer disciplina). Causa do sumiço: sessão manual no SQL Editor (LICAO-041). Verificação + rollback no arquivo. |
| `supabase/migrations/20260715_062_notas_aluno_select.sql` | ✅ **APLICADA** | 2026-07-15 | **Validada. NÃO reexecutar.** Versionada via PR `chore/migrations-061-064`. **Restaura SELECT + DELETE de `notas_aluno`** (produção só com insert+update da 059 → ninguém lia notas: professor não via o que lançou, aluno sem "Minhas Notas", staff nada). SELECT: `aluno_id = auth.uid()` (aluno, Fase C) OU `professor_leciona_disciplina(disciplina_id)` (endurecido vs 031 — antes qualquer professor via TODAS) OU staff via user_roles. DELETE: superadmin-only (forma da 031). INSERT/UPDATE da 059 não tocados. Liga ao aluno por `aluno_id` DIRETO (FK → profiles, 023) — sem passar por matricula. Inclui **levantamento geral** (query pg_policies + conjunto esperado de frequencia/notas_aluno/avaliacoes/matriculas_disciplina/matriculas/profiles) para fechar o "buraco por buraco". Verificação + rollback no arquivo. |

| `supabase/migrations/20260715_063_restaurar_policies_perdidas.sql` | ✅ **APLICADA (v2 aditiva)** | 2026-07-15 | **Validada: avaliacoes=4 · matriculas=5 · profiles=8; pré-existentes intactas. NÃO reexecutar.** Versionada via PR `chore/migrations-061-064`. **Adiciona SÓ as policies que faltam em `avaliacoes`/`matriculas`/`profiles`** (3ª da família 061/062 — LICAO-041). ⚠️ **v1 (recriar verbatim das origens) REPROVADA**: o pg_policies real mostrou policies **evoluídas além das migrações versionadas** — usam `is_staff()` (origem: `.prd/02_profiles_rls.sql`, plpgsql SECURITY DEFINER lendo profiles, roles administracao/admin/superadmin — SEM financeiro/professor) e endurecimentos posteriores (`profiles_update_staff` com lista de roles SEM superadmin = anti-escalação). Recriar da origem desfaria isso. **v2 não toca em NENHUMA existente**; adiciona 7: `avaliacoes` INSERT (autoria + `professor_leciona_disciplina` OU `is_staff()`) / UPDATE (idem) / DELETE (admin/superadmin via user_roles — cascateia em notas) · `matriculas_select_own` (aluno vê a própria — **fecha ERR-DATA-F4**/Plano Mestre F4) · `matriculas_delete_superadmin` · `profiles_insert_staff` (`is_staff()` + guarda anti-escalação: staff não cria superadmin) · `profiles_delete_superadmin` (LGPD). Contagens esperadas pós-run: avaliacoes=4 · matriculas=5 · profiles=8 (update_admin+update_administracao da 033 foram CONSOLIDADAS na update_staff — não são faltantes). Verificação §2 confere pré-existentes byte a byte. Rollback aditivo no arquivo. |

| `supabase/migrations/20260716_064_roster_turma_id.sql` | ✅ **APLICADA** | 2026-07-16 | **Validada: roster retorna `turma_id` (caso A2: Apologética → João com turma `953b4ee8…`). NÃO reexecutar.** Versionada via PR `chore/migrations-061-064`; código consumidor no PR `fix/a2b-turma-roster` (#46, merged). **Roster +turma_id — solução DEFINITIVA do A2 ("Sem turma vinculada" nas Notas).** Confirmado por SQL: Apologética tem `aulas_recorrentes` count=0 → a fonte 1 do fix `fix/a2-turma-notas` volta vazia e o fallback via `matriculas` é bloqueado pela RLS para o professor (sem SELECT em matriculas) → botão de Notas seguia desabilitado. A 064 expõe `m.turma_id` no `get_alunos_operacional` (SECURITY DEFINER — bypassa RLS; mesma fonte gated que o professor já usa em Meus Alunos/Frequência/VerTurma/Consolidado/R02). **DROP + CREATE** (espelha 058/060 — RETURNS muda), `turma_id uuid` após `matricula_id`; gate/filtro/`REVOKE anon` inalterados. **Aditivo** — consumidores mapeiam por nome via RPC, coluna extra ignorada até o código usá-la (passo de código à parte: interface `AlunoOperacional` + `useProfessorDisciplinas` consumir turma do roster). Verificação inclui o caso A2 (roster de Apologética → João com turma `953b4ee8…`). Rollback (volta à 060) no arquivo. |

| `supabase/migrations/20260716_065_trigger_consolidado.sql` | ✅ **APLICADA** | 2026-07-16 | **Fase B — consolidação automática — validada**: 2 triggers instalados (`trg_frequencia_consolida`, `trg_notas_consolida`); consolidado do João recalculado pelo trigger a partir do que o professor lançou = **nota 7.30 · faltas 0 · freq 100 · status `aprovado`**. **NÃO reexecutar.** Ver LICAO-042 (ELO). Detalhe da migração: Função `recalcular_consolidado(aluno_id, disciplina_id)` (plpgsql **SECURITY DEFINER** — obrigatório: professor não tem UPDATE em `matriculas_disciplina`; sem DEFINER a RLS derrubaria o próprio INSERT da chamada) + triggers `trg_frequencia_consolida`/`trg_notas_consolida` (AFTER I/U/D FOR EACH ROW) + **backfill** de todos os pares com lançamento live (nº de pares sai no NOTICE do run — anotar aqui). Régua = espelho EXATO do front (conferida linha a linha): média (N1+N2)/2 1 casa (NULL se faltar nota) · status na ordem `cursando→reprovado_falta(freq<75)→aprovado(≥7)→recuperacao(≥5)→reprovado` · **sem chamada → `frequencia_percentual`/`faltas` = NULL e status calculado com freq 100** (idêntico ao retroativo). **GUARD**: nunca recalcula `trancado`/`convalidado`. SEM `status_manual` (decisão Hélio). Sem loop (função escreve só em md). Verificação: caso João/Apologética → nota 7.5 · faltas 0 · freq 100 · `aprovado`. Rollback (DROP 2 triggers + 2 funções; valores congelam) no arquivo. |

| `supabase/migrations/20260718_066_leads_cursos_campos.sql` | ✅ **APLICADA** | 2026-07-18 | **Validada: 4 colunas criadas; lead de teste gravou (HELIO/teologia-livre/Paulista; total_leads=1). NÃO reexecutar.** Versionada via PR `fix/leads-log-erro`. **12.0 parte 2 — leads_cursos ganha as 4 colunas que o form já coleta.** Causa confirmada por schema: a tabela tinha só 6 colunas de dados e o form "Reservar minha vaga" enviava `cidade`/`como_conheceu`/`mensagem`/`lgpd_aceite` (inexistentes) → PostgREST rejeitava o INSERT inteiro (42703) → **TODO lead falhava** (total_leads=0; ITEC perdendo interessados em silêncio). NÃO era RLS (policy anon da 001 existe). A 066 é **aditiva**: `ADD COLUMN IF NOT EXISTS` cidade/como_conheceu/mensagem (text) + `lgpd_aceite boolean NOT NULL DEFAULT false` — RLS/dados intocados. Código par (mesmo PR `fix/leads-log-erro`): insert com 9 chaves 1:1 com o schema, `criado_em` removido (DEFAULT do banco), fallback localStorage removido, erro acionável com WhatsApp, LeadCaptureModal só mostra sucesso se gravou. Verificação (10 colunas + policies + teste real do form) + rollback (DROP das 4 — apaga dados delas) no arquivo. |

| `supabase/migrations/20260719_067_financeiro_acesso.sql` | ✅ **APLICADA** | 2026-07-19 | **NÃO reexecutar.** Versionada via PR `feat/financeiro-acesso-breno` (junto com o front: FinanceiroView + menu + RoleGuards). **Acesso granular do role `financeiro` (Breno) — LEITURA do acadêmico sem poder de escrita.** O role perdeu acesso quando profiles/matriculas evoluíram p/ `is_staff()` (sem financeiro): não lia nomes de alunos (R04/R05 vazios), gerarMensalidades="0". **Decisão validada: NÃO mexer em `is_staff()`** (guarda escritas sensíveis — `profiles_update_staff` muda ROLE = escalação; matriculas update = aprovar matrícula). Estratégia **aditiva** (padrão 063): função **`is_financeiro()`** (SQL STABLE SECURITY DEFINER lendo profiles — família de is_staff/is_superadmin; REVOKE anon) + **3 policies de SELECT**: `profiles_select_financeiro` · `matriculas_select_financeiro` · `mat_disc_select_financeiro`. Zero DROP em existentes; zero escrita nova (mensalidades/taxa já S/I/U pela 037; DELETE segue admin/super). Turmas/cursos/modulos/disciplinas_v2 já eram legíveis (018/008). Leads FORA (decisão). Verificação por papel (nomes voltam; gerar N>0; negativos: aprovar matrícula/nota/frequencia negados) + rollback no arquivo. **Código par (à parte): FinanceiroView na home + menu + RoleGuards (R05, relatorios, alunos; Ficha 360 = decisão pendente do Hélio).** |

| `supabase/migrations/20260720_068_tabela_precos.sql` | 🟡 **CRIADA (aguardando run)** | 2026-07-20 | **Financeiro Etapa 2a — tabela de preços editável.** CREATE `tabela_precos` **ADITIVA** (mensalidades/comprovante intocados; sem `financeiro_*` no repo): `ano × tipo (disciplinas\|familia) × qtd (1-4)` → 4 valores (matrícula até/após 15/01 · mensalidade até/após dia 10) + `ativo` + autoria (`atualizado_por/em`) + UNIQUE(ano,tipo,qtd). RLS: `precos_select_authenticated` (referência de preço) + `precos_write_gestao` (ALL, `is_staff() OR is_financeiro()`); REVOKE anon. **SEED 2026 idempotente: 8 linhas oficiais** da tabela do Hélio (disciplinas 1→50/125/125/135 … familia 4→200/300/300/330). Verificação (count=8 + papéis) + rollback (DROP — só antes da Etapa 2b) no arquivo. Código par no PR `feat/financeiro-precos`: precos.service + tela TabelaPrecos (grade editável 2 blocos × 4 linhas, seletor de ano, salvar por linha) + rota/menu/botão. |

| `supabase/migrations/20260720_069_matricula_valores.sql` | 🟡 **CRIADA (aguardando run)** | 2026-07-20 | **Financeiro Etapa 2b — valor efetivo por matrícula (override).** Depende da **068**. `matriculas` +`tipo_cobranca` (disciplinas\|familia, default; backfill de tipo_financiamento='familia') + `valor_matricula_override`/`valor_mensalidade_override` (NULL=usa tabela); justificativa reusa `observacao_financeira` (036). **`resolver_valor_efetivo(matricula, ano?)`** SECURITY DEFINER (régua única LICAO-042): conta disciplinas 'cursando' (clamp 1-4) → `tabela_precos` → aplica override → 4 valores + origem (override/tabela/sem_tabela); gate staff/financeiro OU o próprio aluno. **`set_valores_matricula(...)`** SECURITY DEFINER gated `is_staff() OR is_financeiro()` — escreve SÓ os campos de valor (financeiro edita sem ganhar UPDATE amplo em matriculas — preserva fronteira 067). Verificação (colunas+backfill=0; João qtd=1→125/135 'tabela'; override→99.90; papéis) + rollback. Código par no PR `feat/financeiro-override`: financeiro.service (getValoresEfetivos/setValoresMatricula) + `ValoresMatriculaPanel` na FichaAluno. ⚠️ Tensão: financeiro edita via RLS mas a Ficha 360 é fronteira 067 (PII) — entrada PII-free do financeiro fica p/ follow-up. |

| `supabase/migrations/20260721_070_gerar_mensalidades.sql` | 🟡 **CRIADA (aguardando run)** | 2026-07-21 | **Financeiro Etapa 2c — geração de mensalidades pelo valor efetivo + preview.** Depende 068+069. **Corrige BUG CRÍTICO de billing**: a geração antiga (`.upsert` onConflict) dava UPDATE em re-execução → **mensalidade JÁ PAGA voltava a 'pendente' com valor novo**. Agora INSERT `ON CONFLICT (aluno_id,mes_referencia) DO NOTHING` (nunca toca linha existente). `mensalidades` +`valor_com_atraso` (snapshot da multa "após dia 10" na geração; `valor`=base "até dia 10" — doc estável). **`preview_gerar_mensalidades(ano,mes)`** (1 linha/matrícula ativa: valor efetivo via resolver_valor_efetivo/069 + origem + ja_existe) e **`gerar_mensalidades_mes(ano,mes,dia,por)`** (INSERT..SELECT LATERAL resolver; só 'ativa'; sem preço não gera; retorna geradas/ja_existiam/sem_preco) — SECURITY DEFINER, gate `is_staff() OR is_financeiro()`, REVOKE anon. Verificação inclui **teste de idempotência** (2ª execução geradas=0, paga permanece) + rollback. Código par no PR `feat/financeiro-geracao`: service (RPC nova assinatura + previewGerarMensalidades) + aba "Gerar Mensalidades" com preview→confirmar; setup de teste +`supabase.rpc`; testes reescritos. |

| `supabase/migrations/20260721_071_gerar_mensalidades_seletivo.sql` | 🟡 **CRIADA (aguardando run)** | 2026-07-21 | **Financeiro 2c.1 — geração SELETIVA (individual/lote).** Depende 070. **DROP** da `gerar_mensalidades_mes` 4-arg (070) + **CREATE** 5-arg com `p_matricula_ids uuid[] DEFAULT NULL` (NULL/vazio = todas as ativas; lista = só essas, filtro `m.id = ANY(...)`) — espelha o padrão 058 (evita ambiguidade de overload). Idempotência (ON CONFLICT DO NOTHING), gate (`is_staff() OR is_financeiro()`), SECURITY DEFINER, REVOKE anon inalterados. Código par no PR `feat/financeiro-geracao-seletiva` (empilhado sobre 2c): service `gerarMensalidadesMes(...,matriculaIds?)` + aba com checkbox por linha, "selecionar todos", elegíveis marcadas por padrão, não-elegíveis desabilitadas, contador dinâmico, botão "Confirmar geração de X". Testes +2 (337/337). Verificação (só a 5-arg; individual/lote; idempotência) + rollback. |

| `supabase/migrations/20260722_072_set_valor_mensalidade.sql` | 🟡 **CRIADA (aguardando run)** | 2026-07-22 | **Financeiro 2c.2 — edição INLINE do valor na geração (grava override).** Depende 069. Função FOCADA **`set_valor_mensalidade_override(matricula_id, valor)`** SECURITY DEFINER, gate `is_staff() OR is_financeiro()`, REVOKE anon — altera SÓ `valor_mensalidade_override` (NULL = volta à tabela). ⚠️ **Por que função nova e não reusar `set_valores_matricula` (069)**: aquela sobrescreve TODOS os campos e apagaria `observacao_financeira` (justificativa obrigatória REGRAS §4) + `tipo_cobranca` a partir da linha do preview (que não os carrega). Código par no PR `feat/financeiro-valor-inline`: service `setValorMensalidadeOverride` + célula "Até dia X" editável inline (origem→'negociado', total recalcula via carregarPreview). Verificação (grava só a coluna; preserva obs/tipo) + rollback. |

> As migrações 001–046 (numeração antiga) e as paradas em `migrations-manuais/` (038, 039, 046)
> seguem o histórico do `CLAUDE.md` do projeto e o backlog do Plano Mestre §9 — não duplicar aqui
> sem confirmação. Esta tabela cobre a fase atual (Núcleo Acadêmico).
>
> 📌 **2026-07-05:** `migrations-manuais/` (038, 039, 046 — aplicadas manualmente via SQL Editor) foi
> **versionado no repositório** junto com o PASSO 1 da 039 (Edge Function `criar-aluno` enviando
> `email` no `user_metadata`) — branch `chore/versionar-migrations-pendentes`. Fecha parte do
> backlog §9 do Plano Mestre ("versionar migrations 038-046 + Edge Function criar-aluno").

---

## Efeito no estado do banco (pós-047/048)
- `disciplinas_v2.codigo` = código v2 compacto (`B1ATG`…), `UNIQUE`. 46 cadeiras.
- `disciplinas_v2.ativo` existe (default `true`).
- `prerequisitos_v2` é a fonte de pré-requisitos consumida pela UI (CursosAdmin via `cursos.service`):
  24 linhas (13 `prerequisito` + 11 `recomendado`).
- Tabelas legadas `disciplinas` / `prerequisitos_disciplinas` ainda existem (serão depreciadas/derrubadas
  em etapa futura do R0.5 — ver [[known-errors]] ERR-DEBT-002, marcado QUITADO no código).

---
*Mantido por agente-Osabio · ITEC-EAD · 2026-06-20*
